<% icon_sizes = Rails.configuration.serviceworker.icon_sizes %>
{
  "name": "Здесь Легко | Курьерская Cлужба",
  "short_name": "Здесь Легко | Курьерская Cлужба",
  "start_url": "/",
  "icons": [
  <% icon_sizes.map { |s| "#{s}x#{s}" }.each.with_index do |dim, i| %>
    {
      "src": "<%= image_path "/logo.png" %>",
      "sizes": "<%= dim %>",
      "type": "image/png"
    }<%= i == (icon_sizes.length - 1) ? '' : ',' %>
  <% end %>
  ],
  "theme_color": "#FED837",
  "background_color": "#FED837",
  "display": "standalone",
  "orientation": "portrait"
}
